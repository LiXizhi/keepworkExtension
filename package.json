{
  "name": "keepwork-extension-repository",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Keepwork MCP runtime and application build orchestration",
  "license": "MIT",
  "scripts": {
    "install:apps": "npm ci --prefix apps/vscode-extension && npm ci --prefix apps/local-helper",
    "check:layout": "node scripts/check-layout.cjs",
    "check:shared": "tsc -p ./ --noEmit && npm run check:layout",
    "check:all": "npm run check:shared && npm run check --prefix apps/vscode-extension && npm run check --prefix apps/local-helper"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "node-pty": "^1.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
